{{#each comments as |comment|}}
  <!-- <div class="item">
    <img src="{{image.url}}" class="img-constrain carousel-img-handler-class" alt="..." data-id="{{image._id}}">
    <div class="carousel-caption">
    </div>
  </div> -->
<div class="sample-comment" data-id="comment-{{image[2]}}">
  <span class="comment">{{image[0]}}</span>
  <br />
  <span class="commentor">-{{image[1]}}</span>
</div>

{{/each}}
